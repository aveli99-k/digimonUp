"""버전 번호. **여기 한 곳에만** 적는다.

GUI 제목 표시줄, 로그 첫 줄, `--version` 이 모두 이 값을 읽는다. 여러 곳에
적어 두면 릴리스할 때 한두 군데가 빠져서, 사용자가 보는 번호와 실제로 도는
코드가 어긋난다.

릴리스 순서
    1. 이 값을 올린다
    2. 커밋
    3. git tag -a vX.Y.Z
"""

__version__ = "1.2.0"


def version_line() -> str:
    """로그와 화면에 함께 쓰는 한 줄."""
    return f"digimonUp v{__version__}"
